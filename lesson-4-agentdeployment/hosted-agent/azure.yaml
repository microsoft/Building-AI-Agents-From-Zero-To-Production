# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: hosted-agent
metadata:
    template: azd-init@1.21.3

# Using existing Azure resources - no infra provisioning needed
# infra:
#     provider: bicep
#     path: infra
#     module: main

services:
    dev-onboarding:
        project: .
        host: azure.ai.agent
        language: python
        docker:
            path: Dockerfile
